# Frontend

React dashboard workspace for smart building operations.

## Phase 7 Validation

- Run `npm run validate:phase7` to validate the backend-mediated realtime dashboard flow.
- Use `VITE_API_BASE_URL`, `VITE_WS_URL`, `VITE_BUILDING_ID`, `VITE_OPERATOR_TOKEN`, and optional `VITE_WS_RECONNECT_MS` for local phase-7 runs.
